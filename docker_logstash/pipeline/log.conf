input{
    http{
        port => 8080
    }
}

#per rimuovere alcuni campi dal json 
filter {
    if [http][method] != "POST"{
        drop {}
    }

    mutate {
      remove_field => ["event", "url","parameters","get","@version","@timestamp"]
    }
}

output {
  #l'output sarà kafka in formato json sul topic kart_topic
  kafka {
    codec => json
    topic_id => "topicCountries"
    bootstrap_servers => "kafkaServer:9092"
  }
}